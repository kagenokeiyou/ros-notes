# 发现（Discovery）

节点的发现通过 ROS 2 的底层中间件自动进行。可以总结如下：

1. 当节点启动时，它会向同一 ROS 域（通过 [`ROS_DOMAIN_ID`](/start/01-start/03-config-env.md#ros-domain-id) 环境变量设置）网络上的其他节点宣告自己的存在。节点通过向这些宣告提供自身信息来响应，以便建立适当的连接并实现节点间通信。
2. 节点会周期性地宣告自己的存在，以便在初始发现期后与新发现的实体建立连接。
3. 节点在离线时会向其他节点宣告。

节点只有在具有兼容的服务质量设置时才会与其他节点建立连接。
